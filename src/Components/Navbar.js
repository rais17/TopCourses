import React from "react";

const Navbar = () => {
  return (
    <div
      className={`font-sans text-3xl font-bold text-white text-center py-4 bg-bgDark`}
    >
      Top Courses
    </div>
  );
};

export default Navbar;
